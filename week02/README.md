# 2주차

## 오늘 배운 내용

- 가상화 (서버, 베어메탈 / 하이퍼바이저 / 컨테이너 비교)
- WSL2 + Ubuntu 환경 구축
- 리눅스 기본 명령어

## 새로 배운 명령어

```bash
wsl --install -d Ubuntu
wsl -l -v
sudo apt update
sudo apt upgrade -y
pwd
ls -l
ls -a
mkdir
rmdir
chmod
touch
cat
echo
cp
mv
rm
df -h
free -h
history
```
